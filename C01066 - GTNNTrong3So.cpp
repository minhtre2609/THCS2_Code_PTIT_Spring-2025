#include <stdio.h>

int main ()
{
	int a, b, c;
	scanf ("%d %d %d", &a, &b, &c);
	
	int minn = a;
	if (minn >= b)
	{
		minn = b;
		if (minn >= c)
		{
			minn >= c;
		}
	}
	
	printf ("%d", minn);
}
